# Exposes Prometheus inside the cluster and to the Prometheus Ingress.
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: gpu-tenant-orchestrator
spec:
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - name: http
      port: 9090
      targetPort: http
