# Example only. Do not apply this file with placeholder values.
#
# Create a real secret for durable Postgres reservations with:
# kubectl -n gpu-tenant-orchestrator create secret generic gpu-tenant-postgres \
#   --from-literal=PLACEMENT_STORE_BACKEND=postgres \
#   --from-literal=GPU_PLACEMENT_DATABASE_URL='postgresql://USER:PASSWORD@HOST/DB?sslmode=require'
apiVersion: v1
kind: Secret
metadata:
  name: gpu-tenant-postgres
  namespace: gpu-tenant-orchestrator
type: Opaque
stringData:
  PLACEMENT_STORE_BACKEND: postgres
  GPU_PLACEMENT_DATABASE_URL: postgresql://USER:PASSWORD@HOST/DB?sslmode=require
